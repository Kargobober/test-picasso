import React from 'react';
import './index.css';
import { withProviders } from 'app/providers';

const App = () => {
  return (
    <div className="App">
    </div>
  );
};

export default withProviders(App);
