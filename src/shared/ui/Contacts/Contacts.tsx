import React from 'react';

type TProps = {
  all?: boolean;
  gitHub?: boolean;
  hh?: boolean;
  habr?: boolean;
  tg?: boolean;
  mail?: boolean;
};

const Contacts = (props: TProps) => {
  return (
    <div>Contacts</div>
  )
}

export default Contacts;
